// Stub for react-force-graph-2d (broken package.json exports field).
// The ForceDirectedGraph story will render nothing, but the build won't fail.
export default function ForceGraph2D() { return null; }
