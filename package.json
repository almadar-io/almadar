{
  "name": "almadar-project",
  "version": "1.0.0",
  "private": true,
  "description": "Almadar project template - install @almadar/* packages via npm",
  "type": "module",
  "scripts": {
    "validate": "almadar validate",
    "compile": "almadar compile",
    "dev": "almadar dev",
    "test": "almadar test"
  },
  "dependencies": {
    "@almadar/core": "^10.25.0",
    "@almadar/std": "^16.186.0",
    "@almadar/validation": "^2.0.0"
  },
  "devDependencies": {
    "@almadar/cli": "^1.5.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
