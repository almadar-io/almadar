/**
 * Theme Configuration
 * Generated by Orbital Compiler
 *
 * This file is regenerated when compiling a schema with theme definitions.
 *
 * Import this file and pass to ThemeProvider:
 *
 * ```tsx
 * import { THEMES, DEFAULT_THEME } from '@/generated/theme-config';
 * import { ThemeProvider } from '@/context';
 *
 * <ThemeProvider themes={THEMES} defaultTheme={DEFAULT_THEME}>
 *   <App />
 * </ThemeProvider>
 * ```
 */

/** Theme definitions for ThemeProvider */
export const THEMES = [
  { name: 'almadar', displayName: 'Almadar', hasLightMode: true, hasDarkMode: true },
] as const;

export type ThemeName = (typeof THEMES)[number]['name'];

/** Default theme from orbital schema */
export const DEFAULT_THEME = 'almadar';

/** Default color mode */
export const DEFAULT_MODE = 'light' as const;
